apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  annotations: {}
  labels:
    name: crossplane-appcat-job-helm-maintenance
  name: crossplane:appcat:job:helm:maintenance
rules:
  - apiGroups:
      - helm.crossplane.io
    resources:
      - releases
    verbs:
      - patch
      - get
      - list
      - watch
